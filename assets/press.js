(()=>{
async function loadPress(){const target=document.getElementById('pressEntries'),status=document.getElementById('pressStatus');try{const response=await fetch('/index.php?api=press');if(!response.ok)throw Error();const data=await response.json();target.replaceChildren();status.textContent=data.stale?'Showing saved press entries.':data.entries.length?'':'Press coverage will appear here soon.';for(const entry of data.entries){const article=document.createElement('article');article.className='press-entry';const date=document.createElement('span');date.textContent=entry.date;const link=document.createElement('a');link.href=entry.url;link.textContent=entry.title;link.target='_blank';link.rel='noopener';article.append(date,link);target.append(article);}}catch{status.textContent='Press entries could not load. Please try again later.'}}loadPress();setInterval(loadPress,300000);

})();
